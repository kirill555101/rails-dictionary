<!DOCTYPE html>
<html>
  <head>
    <title>Dictionary</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" 
      rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <p class="h5 my-0 me-md-auto fw-normal">Словарь</p>
      <% unless @current_user %>
        <a class="btn btn-outline-primary" href="/login">Войти</a>
      <% else %>
        <span class="p-2">Вы вошли как <span class="text-info"><%= @current_user["name"] %></span></span>
        <a class="btn btn-outline-primary" href="/logout">Выйти</a>
      <% end %>
    </header>
    <div class="container text-center" style="width: 400px;">
      <div class="alert alert-warning" style="top: 20px;" id="errorMessage" hidden>
        <%= flash[:error] %>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
